{% extends 'base.html.twig' %}

{% block title %}Livre{% endblock %}

{% block body %}
     <div class="container">
         <h1 align="center">Livres</h1>
        <div class="row">
            <div class="col-md-3">
             {% include 'livre/filtre.html.twig' %}
            </div>
            <div class="col-md-9">
                <div class="row row-cols-1 row-cols-md-3 g-4">
                {% for livre in livres %}
                    {%  include'livre/card.html.twig' with {livre:livre} only %}
                {% else %}
                    <tr>
                        <td colspan="7">no records found</td>
                    </tr>
                {% endfor %}
                </div>
            </table>
            </div>
        </div>
     </div>
{% endblock %}
